Frame Analysis
Previous frame summaries:

{PREVIOUS_FRAMES}

Analyze only the current frame image. Use previous frame summaries only to track continuity and changes.
Focus on concrete visible evidence.

{prompt}

Return plain text using exactly these sections:

SUMMARY:
<1-2 short factual sentences about the most important visible state or change>

NEW_INFORMATION:
- <new object, person, text, UI change, or event>
- <new object, person, text, UI change, or event>
- Write `- None` if nothing new is visible.

CONTINUITY:
- <1-3 short things the next frame should track>
- Write `- None` if there is nothing specific to track.

VISIBLE_TEXT:
- <exact readable on-screen text>
- Write `- None` if no text is clearly readable.

Rules:
- Keep the whole response under 120 words.
- Stay factual and visual.
- Do not repeat the prompt.
- Do not mention audio unless it is visible on screen.
- Do not use markdown fences.
